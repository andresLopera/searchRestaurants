import React from 'react';
import {Text, SafeAreaView} from 'react-native';
import AppNavigator from './appNavigator';

const AppContainer = props => {
  return <Text>Andres</Text>;
};

export default AppContainer;
